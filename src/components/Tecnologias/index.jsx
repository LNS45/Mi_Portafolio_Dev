import { Box } from "@mui/material";
import {useTheme} from "@mui/material";

const Tecnologias = () => {
    const tema = useTheme();

    const style = {
        width: "100%",
        height: "500px",

    }

    return <Box sx={style}>

    </Box>
};

export default Tecnologias;